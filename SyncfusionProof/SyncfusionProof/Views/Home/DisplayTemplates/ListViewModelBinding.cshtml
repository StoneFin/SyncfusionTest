@model SyncfusionProof.Data.Models.HomeModel

@(Html.EJ().ListView("ListViewModelBinding")
  .DataSource(Model.TestModels)
  .ShowHeader(true)
  .HeaderTitle("TestModels")
  .EnableCheckMark(true)
  .AllowScrolling(true)
  .FieldSettings(x =>
  {
    x.Text("OrderId");
  })
)
