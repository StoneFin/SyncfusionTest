@model SyncfusionProof.Models.HomeModel

@(Html.EJ().ListView("TestModelListView")
  .DataSource(Model.TestModels)
  .ShowHeader(true)
  .HeaderTitle("TestModels")
  .EnableCheckMark(true)
  .AllowScrolling(true)
  .FieldSettings(x =>
  {
    x.Text("OrderId");
  })
)
